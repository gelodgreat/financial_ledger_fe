const APP_ROOT = '/app';
export const MAIN_ROUTES = {
  LANDING: '/',
  LOGIN: `/login`,
  REGISTER: `/register`,
  FORGOT_PASSWORD: `/forgot-password`,
};

export const APP_ROUTES = {
  ROOT: `${APP_ROOT}`,
  FINANCIALS: `${APP_ROOT}/financials`,
  DASHBOARD: `${APP_ROOT}/dashboard`,
};
