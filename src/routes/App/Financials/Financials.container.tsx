import React from 'react';

import FinancialsView from './Financials.view';

const Financials = (): JSX.Element => {
  const generatedProps = {
    // generated props here
  };
  return <FinancialsView {...generatedProps} />;
};

export default Financials;
