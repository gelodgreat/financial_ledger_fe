import React from 'react';

// import { useTheme } from 'utils/Theme';
import { {{ pascalCase name}}Props } from './{{ pascalCase name}}.props';
import { Container } from './{{ pascalCase name}}.style';

const {{ pascalCase name}} = (props: {{ pascalCase name}}Props): JSX.Element => {
  // const theme = useTheme();
  return <Container />;
};

export default React.memo({{ pascalCase name}});
