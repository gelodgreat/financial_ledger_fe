import React from 'react';

// import { useTheme } from 'utils/Theme';
import { {{ pascalCase name}}GeneratedProps } from './{{ pascalCase name}}.props';
import { Container } from './{{ pascalCase name}}.style';

const {{ pascalCase name}}View = (props: {{ pascalCase name}}GeneratedProps): JSX.Element => {
  // const theme = useTheme();
  return <Container />;
};

export default React.memo({{ pascalCase name}}View);
