import React from 'react';

import { {{ pascalCase name}}PublicProps } from './{{ pascalCase name}}.props';
import {{ pascalCase name}}View from './{{ pascalCase name}}.view';

const {{ pascalCase name}} = (props: {{ pascalCase name}}PublicProps): JSX.Element => {
  const generatedProps = {
    // generated props here
  };
  return <{{ pascalCase name}}View {...generatedProps} />;
};

export default {{ pascalCase name}};
